# Build your terraform file using the following commands;
- terraform init 
- terraform validate
- terraform apply

## If you need to remove the infrastructure managed by terraform you can do so using the 
- terraform destroy
